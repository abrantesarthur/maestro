# # cspell:ignore cmdline, workdir
---
ansible-navigator:
  mode: stdout # Output directly to stdout instead of TUI mode
  playbook-artifact:
    enable: False # Disable generating playbook artifact files
  logging:
    level: error # Only show error-level logs
  execution-environment:
    enabled: True # Enable running within an execution environment container
    container-engine: docker
    image: ansible_ee:latest # Name of the execution environment image to run
    pull:
      policy: missing # Only pull the image if it’s not present locally
    environment-variables: # forward host env vars into the execution-environment container
      pass: # names of host env vars to pass through as-is (use `set:` for explicit NAME: value pairs)
        - SSH_HOSTS
        - SSH_KEY_PATH
        - GHCR_TOKEN
        - GHCR_USERNAME
        - BACKEND_IMAGE
        - BACKEND_IMAGE_TAG
        - BACKEND_PORT
  ansible:
    inventory:
      help: False # Do not show inventory help output
      entries:
        # Executable dynamic inventory: Ansible runs this script and parses its stdout as JSON
        # (host list is computed at runtime from the SSH_HOSTS env var, not a static file)
        - inventory/hosts.py
