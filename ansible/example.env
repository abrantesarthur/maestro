# ============================================
# Required Environment Variables
# ============================================
# These are required when deploying backend (not using --skip-backend)

# Full ghcr.io image reference for your backend application
BACKEND_IMAGE=ghcr.io/your-org/your-app

# Image tag to deploy (e.g., latest, v1.0.0, sha-abc123)
BACKEND_IMAGE_TAG=latest

# ============================================
# Optional Environment Variables
# ============================================
# These have sensible defaults or are fetched from other sources

# Bitwarden Secrets Manager project ID (if omitted, fetches from all projects)
# BWS_PROJECT_ID=your_project_id_here

