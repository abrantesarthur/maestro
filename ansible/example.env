# ============================================
# Required Environment Variables
# ============================================
# These are required when deploying backend (not using --skip-backend)

# Full ghcr.io image reference for your backend application
BACKEND_IMAGE=ghcr.io/your-org/your-app

# Image tag to deploy (e.g., latest, v1.0.0, sha-abc123)
BACKEND_IMAGE_TAG=latest

# ============================================
# Backend Container Environment Variables
# ============================================
# Any variable prefixed with BACKEND_ENV_ will be passed to the container
# with the prefix stripped.
# Example: BACKEND_ENV_PORT=3000 becomes PORT=3000 in the container

# Port the backend application listens on (required by most apps)
BACKEND_ENV_PORT=3000

# Add your app-specific environment variables below:
# BACKEND_ENV_DATABASE_URL=postgres://user:pass@host:5432/db
# BACKEND_ENV_REDIS_URL=redis://localhost:6379
# BACKEND_ENV_API_KEY=your_api_key_here

# ============================================
# Bitwarden Secrets Manager
# ============================================
# Comma-separated list of BWS secret names that must be present
# These will be validated before provisioning starts
# Example: BWS_REQUIRED_VARS=MY_API_KEY,DATABASE_PASSWORD,STRIPE_SECRET
# BWS_REQUIRED_VARS=

# ============================================
# Optional Environment Variables
# ============================================
# These have sensible defaults or are fetched from other sources

# Bitwarden Secrets Manager project ID (if omitted, fetches from all projects)
# BWS_PROJECT_ID=your_project_id_here
